fastapi>=0.104.0
uvicorn>=0.24.0
sqlalchemy>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.25.0
python-multipart>=0.0.6
apscheduler>=3.10.0
PyPDF2>=3.0.0
python-docx>=1.0.0
pandas>=2.1.0
aiofiles>=23.0.0
anthropic>=0.20.0
# Multi-tenant auth (Phase 3)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0
bcrypt>=4.0.0,<5.0.0
slowapi>=0.1.9
bcrypt==4.0.1
alembic>=1.13.0
structlog>=24.1.0
# Postgres driver — needed when DATABASE_URL is a postgresql:// URL
# (Supabase, Render Postgres, etc.). -binary ships precompiled wheels so
# Render's build step doesn't need libpq dev headers. SQLite-only deploys
# don't import this; it's a no-op cost.
psycopg2-binary>=2.9.0
# Test suite — pinned to majors that have shipped against our async/Pydantic v2 stack.
# pytest-asyncio drives the @pytest.mark.asyncio + asyncio_mode=auto config.
# pytest-mock supplies the `mocker` fixture used to patch _get_ai_reply_with_fallback
# and httpx.AsyncClient.post without bleeding state between tests.
pytest>=8.0
pytest-asyncio>=0.23
pytest-mock>=3.12
